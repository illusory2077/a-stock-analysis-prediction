# A股分析预测项目规范

## 1. 项目目标

本项目用于获取 A 股及全球主要市场数据，进行大盘/指数分析、个股技术分析、资金面分析、消息面分析和次日（T+1）走势预测，并生成可审阅的中文 Markdown 报告。

主要输出：

- 大盘和主要指数综合研判；
- A 股个股次日方向、概率和价格区间预测；
- 风险提示、交易计划和关键观察条件；
- 数据来源、获取时间和数据质量说明。

## 2. 数据源优先级

### A 股行情和基本面

1. Tushare Pro：主要数据源，优先获取日线、复权因子、财务、资金、龙虎榜和交易日历。
2. AKShare：补充数据源、原型开发和交叉验证。
3. 上海证券交易所、深圳证券交易所及其授权供应商：重要结论和生产级实时数据的核验来源。

### 全球市场

1. Polygon：美股实时、历史和 WebSocket 行情的优先候选。
2. Alpha Vantage：低频全球行情和预算受限时的补充来源。
3. FRED：美国及全球宏观经济数据。
4. SEC EDGAR：美股财报、监管文件和公司披露信息。
5. Tavily：新闻搜索、网页正文提取和事件发现；不得替代交易所公告、公司披露或行情供应商。
6. 对港股、欧洲、日韩等市场，优先使用对应交易所或授权供应商；没有稳定授权源时，必须标注数据延迟和来源限制。

## 3. 实时数据规则

- 明确区分收盘数据、15 分钟延迟、实时快照、实时推送、Tick 和 Level-2 数据。
- 实时行情优先使用具备明确授权和稳定 SLA 的供应商。
- AKShare 或网页接口仅作为研究型/原型数据源，不得默认视为生产级实时行情。
- 实时采集失败时，按数据源优先级切换备用源，并在报告中标记降级情况。
- 不得为了获取数据绕过登录、验证码、限流、访问控制或供应商授权。
- 预测任务应优先使用已经完整收盘的数据；盘中数据必须标注采集时刻。
- Tavily 新闻结果属于搜索/网页研究数据，必须记录来源 URL、发布时间（如有）和检索时间；不得将其默认为实时行情。

## 4. API 密钥和配置

- API Token、密码、Cookie 和私密配置不得写入源代码、Markdown 报告或 Git。
- 优先从环境变量或本地未提交配置文件读取密钥，例如：
  - `TUSHARE_TOKEN`
  - `ALPHAVANTAGE_KEY`
  - `TAVILY_API_KEY`
  - `POLYGON_API_KEY`
- 提交代码前检查是否意外包含密钥。
- 缺少密钥时，明确说明缺少哪个配置，不要伪造数据或静默使用未经确认的替代源。

## 5. 数据标准化

所有行情数据统一转换为内部字段：

```text
symbol
exchange
asset_type
trade_date
timestamp
open
high
low
close
volume
amount
adjust_factor
currency
source
source_record_id
retrieved_at
```

所有数据源都应保留：

- `source`：数据供应商或接口名称；
- `retrieved_at`：实际获取时间；
- `data_version`：数据版本或接口版本（可用时）；
- 原始响应或原始文件，以便审计和复现。

## 6. 时间、交易日和价格口径

- 时间戳统一保存为 UTC，同时保留交易所本地交易日。
- 默认时区：A 股和港股使用 `Asia/Shanghai` 或 `Asia/Hong_Kong`；美股使用 `America/New_York`。
- 使用交易所交易日历，不得用自然日简单补齐行情。
- 明确区分未复权、前复权和后复权价格：
  - 回测和技术指标：记录并明确复权口径；
  - 真实成交和交易计划：优先展示未复权价格；
  - 公司行为分析：保留复权因子。
- 处理停牌、除权除息、分红、拆股、退市和更名情况。

## 7. 数据质量检查

每次入库前至少检查：

- 股票/指数代码和交易所是否正确；
- 时间范围是否符合请求；
- OHLC 是否满足 `high >= max(open, close)`、`low <= min(open, close)`；
- 成交量和成交额是否为空、负数或明显异常；
- 是否存在重复记录；
- 是否发生日期错位、时区错位或复权口径混用；
- 与备用来源的收盘价和成交量差异是否超过阈值。

异常数据不得直接进入预测结果，应记录日志并标记为待核验。

## 8. 防止未来函数

- 财报、公告、新闻和资金数据必须按照公开披露时间进入模型，而不是只按报告期或交易日期进入。
- 预测某个交易日时，只能使用该时点之前已经公开的数据。
- 回测时记录数据截面时间，避免使用后来修订的数据污染历史结果。
- 对成分股、指数权重和退市股票保留历史版本，尽量避免幸存者偏差。

## 9. 分析框架

个股次日预测默认采用四维框架，并可根据数据可用性调整：

- 大盘环境：30%；
- 资金行为：30%；
- 技术面：25%；
- 消息面：15%。

分析必须给出：

1. 数据截止时间；
2. 关键事实和数据来源；
3. 从事实到结论的逻辑链；
4. 看多、看空或震荡判断；
5. 概率和价格区间的依据；
6. 失效条件、止损/风险控制和不确定性。

概率是模型估计，不是收益承诺。不得将预测表述为确定性结论。

## 10. 报告规范

- 报告使用中文，必要时解释专业术语。
- 每份报告必须写明：分析对象、市场、数据截止日期、实际获取时间和数据源。
- 实时数据报告必须标注“实时”“延迟”或“收盘后数据”。
- 建议使用表格、Mermaid、ASCII 或 SVG 展示关键关系，但图表不得替代原始数据说明。
- 个股报告文件名统一为：

```text
a-stock-{代码}-{YYYY-MM-DD}.md
```

- 报告默认保存到 `reports/`；如果项目已有其他目录约定，应遵循现有约定。

## 11. 工程规范

- 数据采集、标准化、存储、指标计算、预测和报告生成应分层实现。
- 为每个供应商实现独立适配器，不要把某个供应商接口散落在业务逻辑中。
- Tavily 仅通过 `src/data_providers/tavily_provider.py` 使用；新闻搜索应限制时间范围、结果数量和必要域名，并保留原始响应。
- 数据源业务调用优先通过 `src/data_providers/router.py`，必须记录主源、备用源、尝试记录和是否降级。
- 新闻原始响应保存到 `data/raw/news/`，标准化去重结果保存到 `data/processed/news/`；每日流水线不得跳过数据截止时间和来源标记。
- 优先使用可复现的脚本和配置，不要手工修改原始数据。
- 网络请求设置超时、重试、限速和日志；重试必须使用退避策略。
- 不因单个数据源失败而伪造完整结果；应降级、标记或停止相关分析。
- 新增依赖前先检查项目现有依赖和 Python 版本。
- 修改代码后运行相关测试或最小可行验证，并在最终说明中报告结果。

## 12. 默认工作流程

1. 检查项目结构、现有规则和可用配置。
2. 确认分析对象、市场、时间范围、数据频率和是否需要实时数据。
3. 按数据源优先级采集数据。
4. 保存原始数据并标准化字段。
5. 执行数据质量检查。
6. 计算技术指标、资金指标和市场环境指标。
7. 结合公告/财报/新闻形成分析结论。
8. 生成报告并标注数据截止时间、来源和限制。
9. 对关键数据和结论进行交叉验证。

## 13. 不确定时的处理

当接口、字段、权限、数据时间或市场状态不明确时：

- 先检查官方文档和项目内已有说明；
- 不凭记忆假设接口仍然可用；
- 报告中明确指出缺失、延迟或未经验证的数据；
- 不用模拟数据冒充真实行情；
- 如果用户明确要求实时数据，先确认实时级别和可用权限。

